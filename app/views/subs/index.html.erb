<h2>All the Subs</h2>

<% @subs.each do |sub| %>
<p>
  <%= link_to sub.title, sub_url(sub) %>
  <% if current_user && current_user.id == sub.author_id %>
    <%= link_to "Edit", edit_sub_url(sub) %>
  <%  end %>
</p>
<% end %>
